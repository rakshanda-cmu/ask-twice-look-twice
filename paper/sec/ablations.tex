\section{Ablations: Isolating the Active Ingredient}
\label{sec:ablations}

The diagnosis in Section~\ref{sec:anatomy} makes a strong claim about what the
second image copy is for. It is not there to be re-perceived. It is there to put
image tokens after the question so the read-out step has something local to
attend to. That claim has a sharp consequence: the echoed copy should not need to
be legible. We test it by degrading the echo, by pricing it, and by asking
whether a prompt optimizer that can only edit text finds the same fix.

\subsection{The echoed copy does not need to be legible}
\label{sec:ablation-resolution}

We downscale only the second image occurrence in \sitit{}, leaving the first at
full resolution, and sweep the echo scale over $1$, $\tfrac{1}{2}$,
$\tfrac{1}{4}$ and $\tfrac{1}{8}$ (Table~\ref{tab:resolution}). On three of four
benchmarks a quarter-resolution echo matches or beats a full-resolution one:
POPE moves by $-0.05$ F1, Winoground by $+0.75$ group accuracy, and NaturalBench
by $+0.21$ pair accuracy. An eighth sharpens the point. On NaturalBench, an echo
carrying $\tfrac{1}{64}$ of the original pixels scores $37.32$ group accuracy
against the full echo's $37.37$, a difference of $0.001$ with $p = 1.00$, while
still beating question-last by $+0.023$ ($p = 0.005$). The echo can be destroyed
as an image and keep its entire benefit.

RF20 is the exception, and the informative one: its accuracy falls at every step
($-0.50$ F1 at half, $-1.76$ at quarter, $-1.88$ at an eighth). It asks whether a
specific, often small object class is present, so the second look does real
perceptual work there rather than only re-anchoring. The split says the echo has
two separable jobs and that only one of them needs pixels.

\begin{figure}[t]
\begin{minipage}[t]{0.50\textwidth}
\vspace{0pt}
\centering
\captionof{table}{The echoed image can be shrunk until it is barely an image
without costing accuracy, except on RF20, where the evidence is small and
accuracy falls at every step. Only the \emph{second} occurrence is downscaled.
Bold marks the best scale per row. Metrics: F1 for POPE and RF20, group accuracy
for Winoground, pair accuracy for NaturalBench.}
\label{tab:resolution}
\vspace{2pt}
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrr}
\toprule
Benchmark & Echo $1$ & $\tfrac{1}{2}$ & $\tfrac{1}{4}$ & $\tfrac{1}{8}$ \\
\midrule
POPE         & 88.12 & \best{88.35} & 88.07 & 87.90 \\
Winoground   & 40.25 & \best{41.75} & 41.00 & \best{41.75} \\
NaturalBench & 80.36 & 80.55 & \best{80.57} & 80.18 \\
RF20 (presence) & \best{80.56} & 80.06 & 78.80 & 78.68 \\
\bottomrule
\end{tabular}
\end{minipage}\hfill
\begin{minipage}[t]{0.47\textwidth}
\vspace{0pt}
\centering
\includegraphics[width=\linewidth]{figs/fig_pareto.pdf}
\caption{Accuracy is flat across a $31\times$ range of echo cost, so the
cheapest echo is the one to ship. NaturalBench; $x$ is extra tokens per query
against question-first, symmetric log so $0$ shows. Every image echo clears the
question-last baseline (dashed), and the $\tfrac{1}{8}$ echo ($+107$ tokens) is
indistinguishable from the full one ($+3{,}352$).}
\label{fig:pareto}
\end{minipage}
\end{figure}

\begin{finding}
\textbf{Finding.} The echoed image does not need to be legible: an
eighth-resolution echo carrying $\tfrac{1}{64}$ of the pixels is statistically
indistinguishable from a full one on NaturalBench ($37.32$ vs $37.37$,
$p = 1.00$) while still beating question-last ($p = 0.005$). Only RF20, whose
targets are small, needs the pixels ($-1.76$ F1 at quarter).
\end{finding}

\subsection{What echoing costs}
\label{sec:ablation-cost}

Prompt-level interventions are usually reported without their bill. We count
exact tokens for each ordering with the Qwen3-VL-8B processor over 20 images
spanning RF20's constituent datasets, separating vision from text tokens
(Table~\ref{tab:cost}, Appendix~\ref{app:cost}).

The two echo mechanisms are three orders of magnitude apart. Repeating the
question costs $13$ tokens; repeating the image at full resolution costs
$3{,}352$, roughly doubling the prompt. Downscaling collapses that cost quickly
because vision-token count falls with area: $875$ added tokens at half, $252$ at
a quarter, $107$ at an eighth. Figure~\ref{fig:pareto} puts the two axes
together, and the accuracy curve is flat across a $31\times$ range of cost, so
the operating point to ship is the cheapest one.

\begin{finding}
\textbf{Finding.} Echo cost is a tunable knob, not a fixed doubling of the
prompt: a full-resolution echo adds $3{,}352$ tokens per query, a quarter adds
$252$ ($7.5\%$) and an eighth adds $107$ ($3.2\%$) for indistinguishable
accuracy, while question echoing adds $13$.
\end{finding}

\subsection{Prompt optimization finds part of the fix, and only where the
mechanism says it should}
\label{sec:ablation-gepa}
\label{sec:gepa}

A reviewer will reasonably ask whether careful prompt engineering would do the
same work as echoing, making the ordering story incidental. We test this with
GEPA \citep{gepa2025}, a reflective prompt optimizer, used as a prediction test
rather than a baseline to beat. The mechanism predicts an asymmetry: GEPA may
rewrite the system message but cannot move the image, so it should find
recoverable headroom under \sti{}, where read-out is broken, and little or
nothing under \sit{}, where it is not. That is what happens (Table~\ref{tab:gepa}, Appendix~\ref{app:gepa}). Under \sti{}, GEPA
improves all three benchmarks: POPE $86.41 \to 87.25$, VQAv2 $86.11 \to 88.13$,
NaturalBench $74.74 \to 75.79$. Under \sit{}, the same optimizer with the same
budget returns the seed prompt unchanged on POPE and VQAv2, both exact ties, and
regresses NaturalBench by $0.41$ by overfitting its validation split.

Two further numbers matter. \sit{}'s baseline is higher than \sti{}'s on every
benchmark ($88.27$ vs $86.41$, $88.55$ vs $86.11$, $78.80$ vs $74.74$), so the
asymmetry is headroom rather than a quirk of the optimizer; and GEPA's best
\sti{} prompt closes only $26\%$ to $83\%$ of the gap, never reaching the plain
\sit{} baseline. Its prompts add $26$ to $71$ tokens per query, less than an
eighth-resolution echo, but its search costs up to $982{,}343$ tokens once plus
a separate reflection model. Rewording recovers part of what bad ordering costs;
moving or echoing the image recovers the rest.

\begin{table}[t]
\caption{A prompt optimizer improves only the ordering our diagnosis says has
headroom, and never catches the ordering it is competing with. GEPA run under
both orderings with the same budget, Qwen3-VL-8B, scored on held-out evaluation
sets of size $N$. Each ordering gives the seed prompt's accuracy and the
optimized prompt's, with the change in parentheses. ``Gap closed'' is the
fraction of the \sti{}-to-\sit{} accuracy gap the optimized \sti{} prompt
recovers.}
\label{tab:gepa}
\begin{center}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrlrrl}
\toprule
 & & \multicolumn{3}{c}{Question-first (\sti{})} & \multicolumn{2}{c}{Question-last (\sit{})} \\
\cmidrule(lr){3-5}\cmidrule(lr){6-7}
Benchmark & $N$ & Seed & GEPA & Gap closed & Seed & GEPA \\
\midrule
POPE         &  8{,}800 & 86.41 & 87.25 ($+0.84$) & $45\%$ & 88.27 & 88.27 ($+0.00$) \\
VQAv2        & 10{,}000 & 86.11 & 88.13 ($+2.02$) & $83\%$ & 88.55 & 88.55 ($+0.00$) \\
NaturalBench &  7{,}000 & 74.74 & 75.79 ($+1.04$) & $26\%$ & 78.80 & 78.39 ($-0.41$) \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{finding}
\textbf{Finding.} Prompt optimization behaves as the read-out diagnosis
predicts: GEPA gains under \sti{} (POPE $+0.84$, VQAv2 $+2.02$, NaturalBench
$+1.04$) and finds nothing under \sit{} (two exact ties and $-0.41$). It closes
$26\%$ to $83\%$ of the gap and never reaches the \sit{} baseline, so rewording
is not a substitute for fixing token order.
\end{finding}
